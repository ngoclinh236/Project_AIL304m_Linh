# Dog Breed Classification

Project sử dụng AlexNet để phân loại giống chó.

## Cách chạy

1. Cài thư viện:
pip install -r requirements.txt

2. Train model:
python train.py

3. Evaluate:
python evaluate.py